<?xml version="1.0" encoding="UTF-8"?>
<uc:ContentButton
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ef="clr-namespace:BudgetBadger.Forms.Effects"
    xmlns:uc="clr-namespace:BudgetBadger.Forms.UserControls"
    x:Class="BudgetBadger.Forms.DataTemplates.MultilineTextColumn"
    Tapped="Handle_Tapped"
    ForceActiveBackground="true"
    Style="{StaticResource DataGridColumnStyle}"
    Padding="0,0,16,0">
    <Editor
        x:Name="TextControl"
        IsReadOnly="{Binding IsReadOnly}"
        IsEnabled="{Binding IsEnabled}"
        AutoSize="TextChanges"
        FontSize="{StaticResource DataGridItemFontSize}"
        Text="{Binding Text}"
        TextColor="{StaticResource PrimaryTextColor}"
        VerticalOptions="CenterAndExpand">
        <Editor.Effects>
            <ef:BorderlessEditorEffect/>
        </Editor.Effects>
    </Editor>
</uc:ContentButton>
