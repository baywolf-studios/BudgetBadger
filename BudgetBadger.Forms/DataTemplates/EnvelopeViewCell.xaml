<?xml version="1.0" encoding="UTF-8"?>
<ViewCell
    x:Name="envelopeCell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="BudgetBadger.Forms.DataTemplates.EnvelopeViewCell"
    Height="{StaticResource Key=ListItemHeight}">
    <Grid
        HeightRequest="{StaticResource Key=ListItemHeight}"
        RowSpacing="0"
        ColumnSpacing="16"
        Margin="-1"
        Padding="1"
        BackgroundColor="{StaticResource BackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>   
        </Grid.ColumnDefinitions>
        <Label
            Grid.Column="0"
            Margin="16,0,0,0"
            Text="{Binding Envelope.Description}"
            Style="{StaticResource Key=ListItemLabelStyle}"/>
        <Label 
            Grid.Column="1"
            Margin="0,0,16,0"
            Text="{Binding Remaining, StringFormat='{0:C}'}"
            Style="{StaticResource Key=ListItemLabelStyle}"
            HorizontalTextAlignment="End"/>
    </Grid>
</ViewCell>
