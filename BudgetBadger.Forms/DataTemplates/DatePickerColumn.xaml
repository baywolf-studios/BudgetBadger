<?xml version="1.0" encoding="UTF-8" ?>
<uc:ContentButton
    x:Class="BudgetBadger.Forms.DataTemplates.DatePickerColumn"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ef="clr-namespace:BudgetBadger.Forms.Effects"
    xmlns:uc="clr-namespace:BudgetBadger.Forms.UserControls"
    Padding="0,0,16,0"
    ForceActiveBackground="true"
    Style="{StaticResource DataGridColumnStyle}"
    Tapped="Handle_Tapped">
    <StackLayout Orientation="Vertical" Spacing="0">
        <DatePicker
            x:Name="DateControl"
            Format="d"
            InputTransparent="{Binding IsReadOnly}"
            IsEnabled="{Binding IsEnabled}"
            VerticalOptions="CenterAndExpand">
            <DatePicker.IsVisible>
                <OnPlatform x:TypeArguments="x:Boolean" Default="true">
                    <On Platform="UWP" Value="false" />
                </OnPlatform>
            </DatePicker.IsVisible>
            <DatePicker.Effects>
                <ef:BorderlessDatePickerEffect />
            </DatePicker.Effects>
        </DatePicker>
        <Entry
            x:Name="TextControl"
            InputTransparent="{Binding IsReadOnly}"
            IsEnabled="{Binding IsEnabled}"
            IsReadOnly="{Binding IsReadOnly}"
            VerticalOptions="CenterAndExpand">
            <Entry.Margin>
                <OnPlatform x:TypeArguments="Thickness" Default="0,0,0,0">
                    <On Platform="UWP" Value="0,8,0,0" />
                </OnPlatform>
            </Entry.Margin>
            <Entry.IsVisible>
                <OnPlatform x:TypeArguments="x:Boolean" Default="false">
                    <On Platform="UWP" Value="true" />
                </OnPlatform>
            </Entry.IsVisible>
            <Entry.Effects>
                <ef:BorderlessEntryEffect />
            </Entry.Effects>
        </Entry>
    </StackLayout>
</uc:ContentButton>
