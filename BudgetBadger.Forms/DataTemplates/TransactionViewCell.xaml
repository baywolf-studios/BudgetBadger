<?xml version="1.0" encoding="UTF-8" ?>
<Grid
    x:Class="BudgetBadger.Forms.DataTemplates.TransactionViewCell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:svg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    ColumnSpacing="16"
    HeightRequest="{StaticResource Key=ListDoubleItemHeight}"
    RowSpacing="4">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="40" />
        <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <ContentView
        Grid.Row="0"
        Grid.Column="0">
        <ContentView.GestureRecognizers>
            <TapGestureRecognizer x:Name="ToggleGestureRecognizer" CommandParameter="{Binding .}" />
        </ContentView.GestureRecognizers>
        <svg:SvgCachedImage
            HeightRequest="24"
            HorizontalOptions="End"
            ReplaceStringMap="{Binding Source={StaticResource PrimaryTextColor}, Converter={StaticResource ReplaceColorMap}}"
            VerticalOptions="Center"
            WidthRequest="24">
            <svg:SvgCachedImage.Triggers>
                <MultiTrigger TargetType="svg:SvgCachedImage">
                    <MultiTrigger.Conditions>
                        <BindingCondition Binding="{Binding Posted}" Value="true" />
                        <BindingCondition Binding="{Binding Reconciled}" Value="false" />
                    </MultiTrigger.Conditions>
                  <Setter Property="Source" Value="posted.svg" />
                </MultiTrigger>
                <DataTrigger
                    Binding="{Binding Reconciled}"
                    TargetType="svg:SvgCachedImage"
                    Value="true">
                    <Setter Property="Source" Value="reconciled.svg" />
                </DataTrigger>
                <DataTrigger
                    Binding="{Binding Pending}"
                    TargetType="svg:SvgCachedImage"
                    Value="true">
                    <Setter Property="Source" Value="pending.svg" />
                </DataTrigger>
            </svg:SvgCachedImage.Triggers>
        </svg:SvgCachedImage>
        
    </ContentView>
    <ContentView Grid.Column="1">
        <ContentView.InputTransparent>
            <OnPlatform x:TypeArguments="x:Boolean">
                <On Platform="Android, macOS" Value="False"/>
                <On Platform="iOS, UWP" Value="True"/>
            </OnPlatform>
        </ContentView.InputTransparent>
        <ContentView.GestureRecognizers>
            <TapGestureRecognizer x:Name="SelectedGestureRecognizer" CommandParameter="{Binding .}" />
        </ContentView.GestureRecognizers>
        <Grid
            RowSpacing="4"
            ColumnSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
            <Label
                x:Name="primaryLabel"
                Grid.Row="0"
                Grid.Column="0"
                Style="{StaticResource Key=ListDoubleItemLabelStyle}"
                Text="{Binding Payee.Description}"
                VerticalOptions="End"/>
            <Label
                x:Name="secondaryLabel"
                Grid.Row="1"
                Grid.Column="0"
                Style="{StaticResource Key=ListDoubleSubItemLabelStyle}"
                Text="{Binding Account.Description}"
                VerticalOptions="Start"/>
            <Label
                x:Name="rightSingleLabel"
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="1"
                Margin="0,0,16,0"
                HorizontalTextAlignment="End"
                Style="{StaticResource Key=ListItemLabelStyle}"
                Text="{Binding Amount, StringFormat='{0:C}'}"
                VerticalOptions="Center"/>
            <Label
                x:Name="rightPrimaryLabel"
                Grid.Row="0"
                Grid.Column="1"
                Margin="0,0,16,0"
                HorizontalTextAlignment="End"
                IsVisible="false"
                Style="{StaticResource Key=ListDoubleItemLabelStyle}"
                Text="{Binding Amount, StringFormat='{0:C}'}"
                VerticalOptions="End"/>
            <Label
                x:Name="rightSecondaryLabel"
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,0,16,0"
                HorizontalTextAlignment="End"
                IsVisible="false"
                Style="{StaticResource Key=ListDoubleSubItemLabelStyle}"
                Text="{Binding Envelope.Description, StringFormat='{0:C}'}"
                VerticalOptions="Start"/>
        </Grid>
    </ContentView>
</Grid>
