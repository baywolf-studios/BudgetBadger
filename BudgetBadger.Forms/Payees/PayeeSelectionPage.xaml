<?xml version="1.0" encoding="UTF-8"?>
<uc:ChildSearchPage
    Title="Select a Payee"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uc="clr-namespace:BudgetBadger.Forms.UserControls"
    x:Class="BudgetBadger.Forms.Payees.PayeeSelectionPage"
    SearchCommand="{Binding SearchCommand}"
    SearchText="{Binding SearchText}">
    <uc:ChildSearchPage.BodyContent>
        <ListView x:Name="PayeeList" BackgroundColor="White" SeparatorVisibility="None" IsPullToRefreshEnabled="true" IsRefreshing="{Binding IsBusy}" RefreshCommand="{Binding RefreshCommand}" ItemsSource="{Binding GroupedPayees}" SelectedItem="{Binding SelectedPayee, Mode=TwoWay}" IsGroupingEnabled="true" GroupDisplayBinding="{Binding Key}">
            <ListView.Behaviors>
                <b:EventToCommandBehavior EventName="ItemSelected" Command="{Binding SelectedCommand}" />
            </ListView.Behaviors>
            <ListView.Header>
                <Button IsVisible="{Binding HasSearchText}" Command="{Binding SaveCommand}" Text="{Binding SearchText, StringFormat='Add Payee: {0}'}"/>
            </ListView.Header>
            <ListView.GroupHeaderTemplate>
                <DataTemplate>
                    <ViewCell Height="48">
                        <StackLayout BackgroundColor="White">
                        <Label
                            HeightRequest="32"
                            FontSize="14"
                            FontFamily="{StaticResource FontFamilyMedium}"
                            VerticalTextAlignment="Center"
                            TextColor="{StaticResource ListViewGroupTextColor}"
                            Text="{Binding Key}"
                            Margin="16, 8"/>
                            </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.GroupHeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Height="48">
                        <Label
                            HeightRequest="32"
                            FontSize="16"
                            FontFamily="{StaticResource FontFamilyRegular}"
                            VerticalTextAlignment="Center"
                            TextColor="{StaticResource PrimaryTextColor}"
                            Text="{Binding Description}"
                            Margin="16, 8"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </uc:ChildSearchPage.BodyContent>
</uc:ChildSearchPage>
