<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:effects="clr-namespace:BudgetBadger.Forms.Effects"
    x:Class="BudgetBadger.Forms.UserControls.ParentHeader">
    <ContentView.Content>
        <Grid x:Name="HeaderGrid" RowSpacing="0" ColumnSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="{StaticResource AppBarIconSize}"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{StaticResource AppBarIconSize}"/>
                <ColumnDefinition Width="{StaticResource AppBarIconSize}"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="{StaticResource AppBarIconSize}"/>
                <ColumnDefinition Width="{StaticResource AppBarIconSize}"/>
            </Grid.ColumnDefinitions>
            <BoxView BackgroundColor="{StaticResource AppBarColor}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"/>
            <Label
                x:Name="LabelControl"
                TextColor="White"
                LineBreakMode="TailTruncation"
                VerticalOptions="Center"
                FontSize="17"
                FontFamily="{StaticResource FontFamilyMedium}"
                Text="{Binding Title}">
                <Grid.Column>
                    <OnPlatform x:TypeArguments="x:Int32">
                        <On Platform="Android" Value="0"/>
                        <On Platform="iOS" Value="2"/>
                        <On Platform="macOS" Value="2"/>
                    </OnPlatform>
                </Grid.Column>
                <Grid.ColumnSpan>
                    <OnPlatform x:TypeArguments="x:Int32">
                        <On Platform="Android" Value="3"/>
                        <On Platform="iOS" Value="1"/>
                        <On Platform="macOS" Value="1"/>
                    </OnPlatform>
                </Grid.ColumnSpan>
                <Label.Margin>
                    <OnPlatform x:TypeArguments="Thickness">
                        <On Platform="Android" Value="16,0,0,0"/>
                        <On Platform="iOS" Value="0"/>
                        <On Platform="macOS" Value="0"/>
                    </OnPlatform>
                </Label.Margin>
                <Label.HorizontalOptions>
                    <OnPlatform x:TypeArguments="LayoutOptions">
                        <On Platform="Android" Value="Start"/>
                        <On Platform="iOS" Value="Center"/>
                        <On Platform="macOS" Value="Center"/>
                    </OnPlatform>
                </Label.HorizontalOptions>
            </Label>
            <Frame
                x:Name="ToolbarItemFrame"
                Grid.Row="0"
                Grid.Column="0"
                HasShadow="false"
                Padding="0"
                Margin="0"
                CornerRadius="0"
                BackgroundColor="Transparent">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ToolbarItem.Command}" />
                </Frame.GestureRecognizers>
                <Image 
                    x:Name="ToolbarItemImage"
                    HeightRequest="24"
                    WidthRequest="24"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"
                    BackgroundColor="Red"
                    Source="{Binding ToolbarItem.Icon}"/>
            </Frame>
                <Frame
                x:Name="SearchBoxFrame"
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="4"
                BackgroundColor="Teal"
                HasShadow="false"
                Padding="0"
                CornerRadius="0"
                TranslationX="{Binding Source={x:Reference SearchBoxFrame}, Path=Width}"
                IsVisible="false">
                <Frame
                    x:Name="EntryFrame"
                    Margin="8"
                    HasShadow="false"
                    Padding="0">
                    <Frame.BackgroundColor>
                        <Color x:FactoryMethod="FromRgba">
                          <x:Arguments>
                            <x:Int32>0</x:Int32>
                            <x:Int32>0</x:Int32>
                            <x:Int32>0</x:Int32>                      
                            <x:Int32>31</x:Int32>
                          </x:Arguments>
                        </Color>
                    </Frame.BackgroundColor>
                    <Entry
                        x:Name="EntryControl"
                        Margin="8,0"
                        VerticalOptions="Center"
                        HorizontalOptions="FillAndExpand"
                        TextColor="White"
                        HeightRequest="{StaticResource AppBarHeight}"
                        FontSize="17"
                        Text="{Binding SearchText}">
                        <Entry.Effects>
                            <effects:BorderlessEntryEffect/>
                        </Entry.Effects>
                    </Entry>
                </Frame>
            </Frame>
            <Frame
                WidthRequest="{StaticResource AppBarHeight}"
                CornerRadius="0"
                HasShadow="false"
                Padding="0"
                HorizontalOptions="End"
                Grid.Row="0"
                Grid.Column="4"
                BackgroundColor="Red">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="SearchTapped" />
                </Frame.GestureRecognizers>
                <Image
                    HeightRequest="24"
                    WidthRequest="24"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    BackgroundColor="Blue"/>
            </Frame>
            <ContentView x:Name="ChildView" Grid.Row="1" Grid.ColumnSpan="5"/>
        </Grid>
    </ContentView.Content>
</ContentView>
