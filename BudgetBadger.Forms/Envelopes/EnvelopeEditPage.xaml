<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Edit Budget" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="BudgetBadger.Forms.Envelopes.EnvelopeEditPage" xmlns:control="clr-namespace:BudgetBadger.Forms.UserControls">
    <ContentPage.Triggers>
        <DataTrigger TargetType="ContentPage" Binding="{Binding Budget.IsNew}" Value="True">
            <Setter Property="Title" Value="New Envelope" />
        </DataTrigger>
    </ContentPage.Triggers>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Command="{Binding SaveCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <control:BusyIndicator IsBusy="{Binding IsBusy}" BusyText="{Binding BusyText}">
            <ScrollView Orientation="Vertical">
                <Grid RowSpacing="16" ColumnSpacing="8" Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <control:ButtonSelector Grid.Row="0" Grid.ColumnSpan="2" Command="{Binding GroupSelectedCommand}" Label="Group" Text="{Binding Budget.Envelope.Group.Description}"/>
                    <control:TextEntry Grid.Row="1" Grid.ColumnSpan="2" Label="Name" Text="{Binding Budget.Envelope.Description}"/>
                    <control:CalculatorEntry Grid.Row="2" Grid.ColumnSpan="2" Prefix="$" Label="Amount" Number="{Binding Budget.Amount}"/>
                    <Label Grid.Row="3" Grid.Column="0" Text="Ignore Overspend"/>
                    <Switch Grid.Row="3" Grid.Column="1" IsToggled="{Binding Budget.IgnoreOverspend}"/>
                    <Label Grid.Row="4" Grid.Column="0" Text="Always"/>
                    <Switch Grid.Row="4" Grid.Column="1" IsToggled="{Binding Budget.Envelope.IgnoreOverspend}"/>
                    <control:MultilineTextEntry Grid.Row="5" Grid.ColumnSpan="2" Label="Notes" Text="{Binding Budget.Envelope.Notes}"/>
                    <Button Grid.Row="6" Grid.ColumnSpan="2" IsVisible="{Binding Budget.IsActive}"  Command="{Binding DeleteCommand}" Text="Delete Envelope" TextColor="Red" />
                </Grid>
            </ScrollView>
        </control:BusyIndicator>
    </ContentPage.Content>
</ContentPage>