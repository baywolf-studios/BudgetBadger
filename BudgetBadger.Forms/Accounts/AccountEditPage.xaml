<?xml version="1.0" encoding="UTF-8"?>
<uc:ChildPage
    Title="Edit Account"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="BudgetBadger.Forms.Accounts.AccountEditPage"
    xmlns:uc="clr-namespace:BudgetBadger.Forms.UserControls"
    ToolbarItemText="Save"
    ToolbarItemCommand="{Binding SaveCommand}"
    ToolbarItemIcon="save.svg">
    <uc:ChildPage.Triggers>
        <DataTrigger TargetType="uc:ChildPage" Binding="{Binding Account.IsNew}" Value="True">
            <Setter Property="Title" Value="New Account" />
        </DataTrigger>
    </uc:ChildPage.Triggers>
    <uc:ChildPage.BodyContent>
        <uc:BusyIndicator IsBusy="{Binding IsBusy}" BusyText="{Binding BusyText}">
            <ScrollView Orientation="Vertical">
                <Grid RowSpacing="16" ColumnSpacing="8" Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="48"/>
                        <RowDefinition Height="48"/>
                        <RowDefinition Height="48"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <uc:TextEntry Grid.Row="0" Label="Name" Text="{Binding Account.Description}" />
                    <uc:DropdownSelector Grid.Row="1" Label="Type" IsEnabled="{Binding Account.IsNew}" SelectedItem="{Binding SelectedAccountType}" ItemsSource="{Binding AccountTypes}" ItemDisplayBinding="{Binding .}"/>
                    <uc:CalculatorEntry Grid.Row="2" Prefix="$" IsEnabled="{Binding Account.IsNew}" Label="Balance" Number="{Binding Account.Balance}" />
                    <uc:MultilineTextEntry Grid.Row="3" Keyboard="Text" Label="Notes" Text="{Binding Account.Notes}"/>
                    <Button Grid.Row="4" IsVisible="{Binding Account.IsActive}" Command="{Binding DeleteCommand}" Text="Delete Account" TextColor="Red" />
                </Grid>
            </ScrollView>
        </uc:BusyIndicator>
    </uc:ChildPage.BodyContent>
</uc:ChildPage>