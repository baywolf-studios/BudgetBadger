using System;
using Xamarin.Forms;

namespace BudgetBadger.Forms.VisualStates
{
    public static class VisualStateEffect
    {
        //      public static readonly BindableProperty IsEnabledProperty =
        //  BindableProperty.CreateAttached("IsEnabled", typeof(bool), typeof(VisualStateEffect), false, propertyChanged: OnHasShadowChanged);

        //      public static bool GetIsEnabled(BindableObject view)
        //      {
        //          return (bool)view.GetValue(HasShadowProperty);
        //      }

        //      public static void SetIsEnabled(BindableObject view, bool value)
        //      {
        //          view.SetValue(HasShadowProperty, value);
        //      }
        //...

        //static void OnHasShadowChanged(BindableObject bindable, object oldValue, object newValue)
        //      {
        //          var view = bindable as View;
        //          if (view == null)
        //          {
        //              return;
        //          }

        //          bool hasShadow = (bool)newValue;
        //          if (hasShadow)
        //          {
        //              view.Effects.Add(new LabelShadowEffect());
        //          }
        //          else
        //          {
        //              var toRemove = view.Effects.FirstOrDefault(e => e is LabelShadowEffect);
        //              if (toRemove != null)
        //              {
        //                  view.Effects.Remove(toRemove);
        //              }
        //          }
        //      }

        //      public VisualStateEffect() : base("Wolf.VisualStateEffect")
        //      {
        //      }
        //  }
    }
}
